services:
  - type: web
    name: optipulse-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: "gunicorn -w 2 -k uvicorn.workers.UvicornWorker backend.main:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
